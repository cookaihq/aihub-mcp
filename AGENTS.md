# aihub-mcp

## Git 提交身份

本仓库提交固定使用：

- `user.name` = `cookaihq`
- `user.email` = `263483889+cookaihq@users.noreply.github.com`

与 remote owner `github.com/cookaihq/aihub-mcp` 一致；仓库将公开分发，使用 GitHub noreply 邮箱避免暴露真实邮箱。已写入仓库级 `git config`，提交前无需再向用户确认身份。
